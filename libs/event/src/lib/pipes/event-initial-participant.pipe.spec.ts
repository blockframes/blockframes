import { EventInitialParticipantPipe } from './event-initial-participant.pipe';

describe('EventInitialParticipantPipe', () => {
  it('create an instance', () => {
    const pipe = new EventInitialParticipantPipe();
    expect(pipe).toBeTruthy();
  });
});
