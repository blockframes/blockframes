=> remove wishlist => non on garde
=> btn add to cart => ké passa après ?
=> rebuy request
=> re-check avails process ? à terminer ? (check process notion.so)
=> basket => cart | distributionDeals => MovieSaleRaw ?
=> stakeholder licensee | licensor

TODO =>
créer un MovieSale (distributiondeal) au lieu d'un DistributionRight

Licensor => va etre cascade8 (mandaté par charade par exemple, info a mettre dans movies)

promotional Element => ajouter langue & pays

Créer le modèle de documents "Contract"

contract obj => date de signature + date range

term => evenement de debut + periode ( 7mois après la sortie en cinema + 3 mois de durée) (evenement flotant) || date début date de fin