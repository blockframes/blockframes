/// <reference types="cypress" />


describe('User create a screening', () => {
  beforeEach(() => {
    
  });

  it('Test', () => {
    const u = cy.getFixture('user');
    cy.log(u);
    console.log(u);
  })

});
